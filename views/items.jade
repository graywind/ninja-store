extends layout

block content
  #wrapper
    #logo
      img(src='/images/logo.jpg')
    #display
      include userbar
        
      -for (var id in items)
        - var item = items[id]
        div
          a(href='/item/#{item._id}') #{item._source.name} - $#{item._source.price}
      
    include footer
